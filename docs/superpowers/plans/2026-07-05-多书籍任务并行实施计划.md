# 多书籍任务并行 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让桌面端可以同时运行多本书的提取任务，同时保证同一本书任意时刻最多只有一个任务在运行。

**Architecture:** 在桌面端主进程增加一个轻量任务调度器，负责全局并行槽位和 `bookId` 互斥；单个任务内部仍沿用当前 `windowRunService` 的窗口串行执行。并行数量、同书互斥上限和队列提示文案进入 `packages/config`，`p0Handlers` 只负责把用户点击的任务提交给调度器，不在 handler 内硬编码策略。

**Tech Stack:** Electron main process、TypeScript、Vitest、`@novel-extractor/config`、现有 IPC `jobs:*` 通道、现有 `jobs:updated` 事件。

---

## 需求边界

- 多本书可以并行跑。
- 同一本书同时只能跑一个任务。
- 默认最大并行任务数为 2。
- 配置允许的最大并行任务数先固定为 3。
- 单个任务内部的窗口和模板批次仍保持串行，不做窗口并行。
- 第二个同书任务点击开始后自动排队，不直接报错。
- 排队任务在应用重启后不自动恢复排队；持久化状态仍保持可再次点击开始。

## 非目标

- 不做同一任务内部窗口并行。
- 不做供应商级 RPM/TPM 限速器。
- 不做前端设置页里的并行数可视化配置。
- 不重写 `windowRunService.ts` 的工具循环、覆盖索引或报告写入逻辑。
- 不改变报告目录和 coverage index 的 key 语义。
- 不自动提交代码；执行任务时如需提交，必须等用户明确授权。

## 文件边界

- Modify: `packages/config/src/schema.ts`
  - 新增 `JobSchedulerDefaults` 配置结构。
  - 在 `NovelExtractorConfig` 中新增 `jobSchedulerDefaults`。
- Modify: `packages/config/src/defaults.ts`
  - 配置默认并行数 2、允许上限 3、同书并行上限 1、排队提示文案。
- Modify: `packages/config/src/defaults.test.ts`
  - 固定默认调度配置。
- Modify: `packages/config/src/configInvariants.ts`
  - 校验调度配置是正整数、默认值不超过允许上限、同书上限固定为 1。
- Modify: `packages/config/src/configInvariants.test.ts`
  - 覆盖非法默认值、非法上限、非法同书上限和空文案。
- Create: `apps/desktop/src/main/jobScheduler.ts`
  - 桌面端全局调度器，负责多书并行、同书互斥、排队和去重启动。
- Create: `apps/desktop/src/main/jobScheduler.test.ts`
  - 单元测试调度器的并行、同书互斥、排队顺序、删除排队任务和重复启动复用。
- Modify: `apps/desktop/src/main/p0Handlers.ts`
  - 接入调度器，替换 `activeJobRuns` 的跨任务启动逻辑。
  - `jobs:start` / `jobs:resume` / `jobs:restart` 改为提交调度器后立即返回最新 `JobDto`。
  - 排队任务使用 `status: "created"` 和配置化 `progressText` 表示等待状态。
- Modify: `apps/desktop/src/main/p0Handlers.test.ts`
  - 增加跨书并行、同书互斥、同 job 重复点击不重复运行的集成测试。
  - 调整依赖旧同步完成语义的测试，显式等待任务完成。
- Modify: `apps/desktop/src/renderer/features/extraction/ExtractionPage.test.tsx`
  - 覆盖排队任务卡显示 `progressText`，保持现有 pending 筛选可见。
- Modify: `apps/desktop/src/renderer/App.test.tsx`
  - 覆盖 `jobs:start` 立即返回 running/created 后，后续通过 `jobs:updated` 更新任务。

---

### Task 1: 配置化任务调度策略

**Files:**
- Modify: `packages/config/src/schema.ts`
- Modify: `packages/config/src/defaults.ts`
- Modify: `packages/config/src/defaults.test.ts`
- Modify: `packages/config/src/configInvariants.ts`
- Modify: `packages/config/src/configInvariants.test.ts`

- [ ] **Step 1: 写 RED 测试固定默认调度配置**

在 `packages/config/src/defaults.test.ts` 增加：

```ts
it("provides job scheduler defaults for multi-book concurrency", () => {
  expect(getDefaultConfig().jobSchedulerDefaults).toEqual({
    maxConcurrentJobs: 2,
    maxAllowedConcurrentJobs: 3,
    maxConcurrentJobsPerBook: 1,
    queuedByGlobalLimitText: "等待可用运行槽",
    queuedByBookLimitText: "等待同书任务完成"
  });
});
```

运行：

```powershell
rtk pnpm --filter @novel-extractor/config test -- defaults.test.ts
```

Expected: FAIL，原因是 `jobSchedulerDefaults` 尚不存在。

- [ ] **Step 2: 写 RED invariant 测试**

在 `packages/config/src/configInvariants.test.ts` 增加 helper 和用例：

```ts
function withJobSchedulerDefaults(
  overrides: Partial<NovelExtractorConfig["jobSchedulerDefaults"]>
): NovelExtractorConfig {
  const config = getDefaultConfig();
  config.jobSchedulerDefaults = {
    maxConcurrentJobs: 2,
    maxAllowedConcurrentJobs: 3,
    maxConcurrentJobsPerBook: 1,
    queuedByGlobalLimitText: "等待可用运行槽",
    queuedByBookLimitText: "等待同书任务完成",
    ...overrides
  };
  return config;
}

it("requires valid job scheduler concurrency limits", () => {
  expectInvariantViolation(withJobSchedulerDefaults({ maxConcurrentJobs: 0 }), /job scheduler max concurrent jobs/i);
  expectInvariantViolation(withJobSchedulerDefaults({ maxAllowedConcurrentJobs: 0 }), /job scheduler max allowed concurrent jobs/i);
  expectInvariantViolation(
    withJobSchedulerDefaults({ maxConcurrentJobs: 4, maxAllowedConcurrentJobs: 3 }),
    /job scheduler max concurrent jobs/i
  );
  expectInvariantViolation(withJobSchedulerDefaults({ maxConcurrentJobsPerBook: 2 }), /job scheduler per book/i);
});

it("requires job scheduler queue labels to be configured", () => {
  expectInvariantViolation(withJobSchedulerDefaults({ queuedByGlobalLimitText: " " }), /global limit queue text/i);
  expectInvariantViolation(withJobSchedulerDefaults({ queuedByBookLimitText: "" }), /book limit queue text/i);
});
```

运行：

```powershell
rtk pnpm --filter @novel-extractor/config test -- configInvariants.test.ts
```

Expected: FAIL，原因是类型和 invariant 尚未实现。

- [ ] **Step 3: 扩展 schema**

在 `packages/config/src/schema.ts` 增加：

```ts
export interface JobSchedulerDefaults {
  maxConcurrentJobs: number;
  maxAllowedConcurrentJobs: number;
  maxConcurrentJobsPerBook: 1;
  queuedByGlobalLimitText: string;
  queuedByBookLimitText: string;
}
```

并在 `NovelExtractorConfig` 中加入：

```ts
jobSchedulerDefaults: JobSchedulerDefaults;
```

- [ ] **Step 4: 增加默认配置**

在 `packages/config/src/defaults.ts` 的 `defineNovelExtractorConfig({ ... })` 顶层加入：

```ts
jobSchedulerDefaults: {
  maxConcurrentJobs: 2,
  maxAllowedConcurrentJobs: 3,
  maxConcurrentJobsPerBook: 1,
  queuedByGlobalLimitText: "等待可用运行槽",
  queuedByBookLimitText: "等待同书任务完成"
},
```

- [ ] **Step 5: 增加 invariant 校验**

在 `packages/config/src/configInvariants.ts` 增加函数：

```ts
function assertJobSchedulerDefaults(config: NovelExtractorConfig): void {
  const defaults = config.jobSchedulerDefaults;
  assertConfigObject(defaults, "job scheduler defaults");
  assertPositiveInteger(defaults.maxConcurrentJobs, "job scheduler max concurrent jobs");
  assertPositiveInteger(defaults.maxAllowedConcurrentJobs, "job scheduler max allowed concurrent jobs");
  assertPositiveInteger(defaults.maxConcurrentJobsPerBook, "job scheduler per book concurrent jobs");
  assertNonEmpty(defaults.queuedByGlobalLimitText, "global limit queue text");
  assertNonEmpty(defaults.queuedByBookLimitText, "book limit queue text");

  if (defaults.maxConcurrentJobs > defaults.maxAllowedConcurrentJobs) {
    throw new ConfigInvariantError(
      "job scheduler max concurrent jobs must not exceed max allowed concurrent jobs."
    );
  }

  if (defaults.maxConcurrentJobsPerBook !== 1) {
    throw new ConfigInvariantError("job scheduler per book concurrent jobs must be 1.");
  }
}
```

在 `assertValidConfigInvariants` 中调用：

```ts
assertJobSchedulerDefaults(config);
```

- [ ] **Step 6: 运行配置包测试**

Run:

```powershell
rtk pnpm --filter @novel-extractor/config test
rtk pnpm --filter @novel-extractor/config typecheck
```

Expected: PASS。

---

### Task 2: 新增桌面端 JobScheduler

**Files:**
- Create: `apps/desktop/src/main/jobScheduler.ts`
- Create: `apps/desktop/src/main/jobScheduler.test.ts`

- [ ] **Step 1: 写 RED 测试，证明不同书最多并行 2 个**

创建 `apps/desktop/src/main/jobScheduler.test.ts`，先写 deferred helper 和第一个测试：

```ts
import { describe, expect, it, vi } from "vitest";
import { createJobScheduler, type ScheduledJob } from "./jobScheduler";

function createDeferred<T = void>() {
  let resolve!: (value: T) => void;
  const promise = new Promise<T>((promiseResolve) => {
    resolve = promiseResolve;
  });
  return { promise, resolve };
}

function job(id: string, bookId: string): ScheduledJob {
  return { id, bookId, createdAt: `2026-07-05T00:00:0${id.at(-1) ?? "0"}.000Z` };
}

describe("desktop job scheduler", () => {
  it("runs at most two different-book jobs concurrently and queues the third", async () => {
    const first = createDeferred();
    const second = createDeferred();
    const third = createDeferred();
    const run = vi
      .fn()
      .mockReturnValueOnce(first.promise)
      .mockReturnValueOnce(second.promise)
      .mockReturnValueOnce(third.promise);
    const queued = vi.fn();
    const scheduler = createJobScheduler({
      maxConcurrentJobs: 2,
      maxConcurrentJobsPerBook: 1,
      run,
      onQueued: queued
    });

    await scheduler.enqueue(job("job-1", "book-1"));
    await scheduler.enqueue(job("job-2", "book-2"));
    await scheduler.enqueue(job("job-3", "book-3"));

    expect(run).toHaveBeenCalledTimes(2);
    expect(queued).toHaveBeenCalledWith(job("job-3", "book-3"), "global_limit");

    first.resolve();
    await vi.waitFor(() => expect(run).toHaveBeenCalledTimes(3));
    second.resolve();
    third.resolve();
  });
});
```

运行：

```powershell
rtk pnpm --filter @novel-extractor/desktop test -- jobScheduler.test.ts
```

Expected: FAIL，原因是 `jobScheduler.ts` 不存在。

- [ ] **Step 2: 补同书互斥 RED 测试**

继续在同一文件增加：

```ts
it("keeps same-book jobs queued while allowing another book to use the free slot", async () => {
  const first = createDeferred();
  const second = createDeferred();
  const third = createDeferred();
  const started: string[] = [];
  const run = vi.fn(async (scheduledJob: ScheduledJob) => {
    started.push(scheduledJob.id);
    if (scheduledJob.id === "job-1") {
      await first.promise;
    } else if (scheduledJob.id === "job-2") {
      await second.promise;
    } else {
      await third.promise;
    }
  });
  const queued = vi.fn();
  const scheduler = createJobScheduler({
    maxConcurrentJobs: 2,
    maxConcurrentJobsPerBook: 1,
    run,
    onQueued: queued
  });

  await scheduler.enqueue(job("job-1", "book-1"));
  await scheduler.enqueue(job("job-2", "book-1"));
  await scheduler.enqueue(job("job-3", "book-2"));

  expect(started).toEqual(["job-1", "job-3"]);
  expect(queued).toHaveBeenCalledWith(job("job-2", "book-1"), "book_limit");

  first.resolve();
  await vi.waitFor(() => expect(started).toEqual(["job-1", "job-3", "job-2"]));
  second.resolve();
  third.resolve();
});
```

Expected: FAIL。

- [ ] **Step 3: 补重复启动和删除排队任务 RED 测试**

继续增加：

```ts
it("deduplicates repeated enqueue calls for the same job", async () => {
  const hold = createDeferred();
  const run = vi.fn(() => hold.promise);
  const scheduler = createJobScheduler({
    maxConcurrentJobs: 2,
    maxConcurrentJobsPerBook: 1,
    run
  });

  await scheduler.enqueue(job("job-1", "book-1"));
  await scheduler.enqueue(job("job-1", "book-1"));

  expect(run).toHaveBeenCalledTimes(1);
  hold.resolve();
});

it("removes a queued job before it starts", async () => {
  const first = createDeferred();
  const run = vi.fn(async (scheduledJob: ScheduledJob) => {
    if (scheduledJob.id === "job-1") {
      await first.promise;
    }
  });
  const scheduler = createJobScheduler({
    maxConcurrentJobs: 1,
    maxConcurrentJobsPerBook: 1,
    run
  });

  await scheduler.enqueue(job("job-1", "book-1"));
  await scheduler.enqueue(job("job-2", "book-2"));
  expect(scheduler.remove("job-2")).toBe(true);
  first.resolve();
  await Promise.resolve();

  expect(run).toHaveBeenCalledTimes(1);
});
```

Expected: FAIL。

- [ ] **Step 4: 实现调度器**

创建 `apps/desktop/src/main/jobScheduler.ts`：

```ts
export type QueueBlockReason = "global_limit" | "book_limit";

export interface ScheduledJob {
  id: string;
  bookId: string;
  createdAt: string;
}

export interface EnqueueResult {
  state: "running" | "queued";
  reason?: QueueBlockReason;
}

export interface JobSchedulerOptions<TJob extends ScheduledJob> {
  maxConcurrentJobs: number;
  maxConcurrentJobsPerBook: number;
  run(job: TJob): Promise<unknown>;
  onQueued?(job: TJob, reason: QueueBlockReason): Promise<void> | void;
  onStarted?(job: TJob): Promise<void> | void;
  onSettled?(job: TJob): Promise<void> | void;
}

interface QueueEntry<TJob extends ScheduledJob> {
  job: TJob;
  sequence: number;
  reason?: QueueBlockReason;
}

interface RunningEntry<TJob extends ScheduledJob> {
  job: TJob;
  promise: Promise<void>;
}

export interface JobScheduler<TJob extends ScheduledJob> {
  enqueue(job: TJob): Promise<EnqueueResult>;
  remove(jobId: string): boolean;
  isRunning(jobId: string): boolean;
  isQueued(jobId: string): boolean;
  getRunningJobIds(): string[];
  getQueuedJobIds(): string[];
}

function assertSchedulerLimit(value: number, label: string): void {
  if (!Number.isInteger(value) || value < 1) {
    throw new Error(`${label} must be a positive integer`);
  }
}

export function createJobScheduler<TJob extends ScheduledJob>(
  options: JobSchedulerOptions<TJob>
): JobScheduler<TJob> {
  assertSchedulerLimit(options.maxConcurrentJobs, "maxConcurrentJobs");
  assertSchedulerLimit(options.maxConcurrentJobsPerBook, "maxConcurrentJobsPerBook");

  const queued = new Map<string, QueueEntry<TJob>>();
  const running = new Map<string, RunningEntry<TJob>>();
  const runningCountByBookId = new Map<string, number>();
  let nextSequence = 0;
  let draining = false;

  function getRunningBookCount(bookId: string): number {
    return runningCountByBookId.get(bookId) ?? 0;
  }

  function getBlockReason(job: TJob): QueueBlockReason | undefined {
    if (getRunningBookCount(job.bookId) >= options.maxConcurrentJobsPerBook) {
      return "book_limit";
    }
    if (running.size >= options.maxConcurrentJobs) {
      return "global_limit";
    }
    return undefined;
  }

  function addRunning(job: TJob, promise: Promise<void>): void {
    running.set(job.id, { job, promise });
    runningCountByBookId.set(job.bookId, getRunningBookCount(job.bookId) + 1);
  }

  function removeRunning(job: TJob): void {
    running.delete(job.id);
    const nextCount = Math.max(0, getRunningBookCount(job.bookId) - 1);
    if (nextCount === 0) {
      runningCountByBookId.delete(job.bookId);
    } else {
      runningCountByBookId.set(job.bookId, nextCount);
    }
  }

  function sortedQueuedEntries(): Array<QueueEntry<TJob>> {
    return [...queued.values()].sort((left, right) => left.sequence - right.sequence);
  }

  function startEntry(entry: QueueEntry<TJob>): void {
    queued.delete(entry.job.id);
    const promise = (async () => {
      try {
        await options.onStarted?.(entry.job);
        await options.run(entry.job);
      } finally {
        removeRunning(entry.job);
        await options.onSettled?.(entry.job);
        void drain();
      }
    })();

    addRunning(entry.job, promise);
    void promise.catch(() => undefined);
  }

  async function drain(): Promise<void> {
    if (draining) {
      return;
    }
    draining = true;
    try {
      let started = true;
      while (started && running.size < options.maxConcurrentJobs) {
        started = false;
        for (const entry of sortedQueuedEntries()) {
          const reason = getBlockReason(entry.job);
          if (reason) {
            entry.reason = reason;
            continue;
          }
          startEntry(entry);
          started = true;
          break;
        }
      }
    } finally {
      draining = false;
    }
  }

  return {
    async enqueue(job) {
      if (running.has(job.id)) {
        return { state: "running" };
      }

      const existingQueued = queued.get(job.id);
      if (existingQueued) {
        return { state: "queued", reason: existingQueued.reason ?? getBlockReason(job) ?? "global_limit" };
      }

      const entry: QueueEntry<TJob> = { job, sequence: nextSequence += 1 };
      queued.set(job.id, entry);
      await drain();

      if (running.has(job.id)) {
        return { state: "running" };
      }

      entry.reason = getBlockReason(job) ?? "global_limit";
      await options.onQueued?.(job, entry.reason);
      return { state: "queued", reason: entry.reason };
    },
    remove(jobId) {
      return queued.delete(jobId);
    },
    isRunning(jobId) {
      return running.has(jobId);
    },
    isQueued(jobId) {
      return queued.has(jobId);
    },
    getRunningJobIds() {
      return [...running.keys()];
    },
    getQueuedJobIds() {
      return sortedQueuedEntries().map((entry) => entry.job.id);
    }
  };
}
```

- [ ] **Step 5: 运行调度器测试**

Run:

```powershell
rtk pnpm --filter @novel-extractor/desktop test -- jobScheduler.test.ts
rtk pnpm --filter @novel-extractor/desktop typecheck
```

Expected: PASS。

---

### Task 3: p0Handlers 接入调度器

**Files:**
- Modify: `apps/desktop/src/main/p0Handlers.ts`
- Modify: `apps/desktop/src/main/p0Handlers.test.ts`

- [ ] **Step 1: 写 RED 集成测试，证明两本书可以并行启动**

在 `apps/desktop/src/main/p0Handlers.test.ts` 增加 deferred helper：

```ts
function createDeferred<T = void>() {
  let resolve!: (value: T) => void;
  const promise = new Promise<T>((promiseResolve) => {
    resolve = promiseResolve;
  });
  return { promise, resolve };
}
```

增加测试。测试中上传两本书，创建两个任务，mock server 的前两个请求保持 pending，确认两个任务都进入请求阶段：

```ts
it("starts jobs for different books concurrently up to the configured limit", async () => {
  const firstResponse = createDeferred<ReturnType<typeof createChatCompletionResponse>>();
  const secondResponse = createDeferred<ReturnType<typeof createChatCompletionResponse>>();
  const mockServer = await startMockOpenAiServer({
    expectedApiKey: "sk-parallel-books",
    respond: ({ requestIndex }) => {
      if (requestIndex === 0) {
        return firstResponse.promise.then((body) => ({ body }));
      }
      if (requestIndex === 1) {
        return secondResponse.promise.then((body) => ({ body }));
      }
      return { body: createChatCompletionResponse({ content: "NO_UPDATE" }) };
    }
  });
  const contract = createIpcContract();
  const { credentialStore, apiKeyRef } = createCredentialFixture("sk-parallel-books");
  const handlers = createHandlers({
    credentialStore,
    providerStore: createProviderStore(createProviderConfig({ apiKeyRef, baseUrl: mockServer.baseUrl }))
  });

  try {
    const firstBook = await contract.invoke(handlers, "books:uploadTxt", {
      projectId: "project-a",
      filePath: utf8FixturePath,
      displayName: "第一本书.txt"
    });
    const secondBook = await contract.invoke(handlers, "books:uploadTxt", {
      projectId: "project-a",
      filePath: gbkFixturePath,
      displayName: "第二本书.txt"
    });
    const firstJob = await contract.invoke(handlers, "jobs:create", {
      bookId: firstBook.bookId,
      templateIds: ["pill-analysis"],
      providerConfigId: "provider-1",
      modelId: "mock-model",
      singleRunChapterCount: 2,
      extractionChapterCount: 2,
      overlapChapterCount: 1,
      skipAlreadyExtracted: true
    });
    const secondJob = await contract.invoke(handlers, "jobs:create", {
      bookId: secondBook.bookId,
      templateIds: ["pill-analysis"],
      providerConfigId: "provider-1",
      modelId: "mock-model",
      singleRunChapterCount: 2,
      extractionChapterCount: 2,
      overlapChapterCount: 1,
      skipAlreadyExtracted: true
    });

    const firstStart = contract.invoke(handlers, "jobs:start", { jobId: firstJob.id });
    const secondStart = contract.invoke(handlers, "jobs:start", { jobId: secondJob.id });

    await expect(firstStart).resolves.toMatchObject({ id: firstJob.id, status: "running" });
    await expect(secondStart).resolves.toMatchObject({ id: secondJob.id, status: "running" });
    await vi.waitFor(() => expect(mockServer.requests).toHaveLength(2));

    firstResponse.resolve(createChatCompletionResponse({ content: "NO_UPDATE" }));
    secondResponse.resolve(createChatCompletionResponse({ content: "NO_UPDATE" }));
  } finally {
    await mockServer.close();
  }
});
```

Expected: FAIL。当前实现虽然可同时调用 handler，但没有全局调度器语义，且 `jobs:start` 会等待任务完成。

- [ ] **Step 2: 写 RED 集成测试，证明同一本书第二个任务排队**

增加测试：同一本书两个任务 + 另一本书一个任务。前两个可运行槽应被第一本书第一个任务和另一本书任务占用，同书第二任务应保持 `created` 且显示配置化等待文案。

```ts
it("queues a second job for the same book while another book can run", async () => {
  const heldResponses = [createDeferred(), createDeferred()];
  const mockServer = await startMockOpenAiServer({
    expectedApiKey: "sk-book-lock",
    respond: ({ requestIndex }) => {
      if (requestIndex < heldResponses.length) {
        return heldResponses[requestIndex].promise.then(() => ({
          body: createChatCompletionResponse({ content: "NO_UPDATE" })
        }));
      }
      return { body: createChatCompletionResponse({ content: "NO_UPDATE" }) };
    }
  });
  const contract = createIpcContract();
  const { credentialStore, apiKeyRef } = createCredentialFixture("sk-book-lock");
  const handlers = createHandlers({
    credentialStore,
    providerStore: createProviderStore(createProviderConfig({ apiKeyRef, baseUrl: mockServer.baseUrl }))
  });

  try {
    const firstBook = await contract.invoke(handlers, "books:uploadTxt", {
      projectId: "project-a",
      filePath: utf8FixturePath,
      displayName: "同书.txt"
    });
    const secondBook = await contract.invoke(handlers, "books:uploadTxt", {
      projectId: "project-a",
      filePath: gbkFixturePath,
      displayName: "别的书.txt"
    });
    const createInput = {
      templateIds: ["pill-analysis"],
      providerConfigId: "provider-1",
      modelId: "mock-model",
      singleRunChapterCount: 2,
      extractionChapterCount: 2,
      overlapChapterCount: 1,
      skipAlreadyExtracted: true
    };
    const sameBookFirst = await contract.invoke(handlers, "jobs:create", {
      ...createInput,
      bookId: firstBook.bookId
    });
    const sameBookSecond = await contract.invoke(handlers, "jobs:create", {
      ...createInput,
      bookId: firstBook.bookId
    });
    const otherBook = await contract.invoke(handlers, "jobs:create", {
      ...createInput,
      bookId: secondBook.bookId
    });

    await contract.invoke(handlers, "jobs:start", { jobId: sameBookFirst.id });
    const queuedSameBook = requireJobDto(await contract.invoke(handlers, "jobs:start", { jobId: sameBookSecond.id }));
    await contract.invoke(handlers, "jobs:start", { jobId: otherBook.id });

    expect(queuedSameBook).toMatchObject({
      id: sameBookSecond.id,
      status: "created",
      progressText: "等待同书任务完成"
    });
    await vi.waitFor(() => expect(mockServer.requests).toHaveLength(2));

    heldResponses[0].resolve();
    await vi.waitFor(() => expect(mockServer.requests.length).toBeGreaterThanOrEqual(3));
    heldResponses[1].resolve();
  } finally {
    await mockServer.close();
  }
});
```

Expected: FAIL。

- [ ] **Step 3: 给 handler options 加调度配置覆盖**

在 `P0IpcHandlersOptions` 中增加测试可覆盖项：

```ts
jobSchedulerDefaults?: Partial<NovelExtractorConfig["jobSchedulerDefaults"]>;
```

在文件顶部补充类型导入：

```ts
import { getDefaultConfig, getTaskStatusConfig, type TaskAction, type NovelExtractorConfig } from "@novel-extractor/config";
```

- [ ] **Step 4: 实例化调度器**

在 `createP0IpcHandlers` 内生成配置：

```ts
const schedulerDefaults = {
  ...getDefaultConfig().jobSchedulerDefaults,
  ...options.jobSchedulerDefaults
};
```

删除或收缩 `activeJobRuns` 的跨任务职责，新增：

```ts
const jobScheduler = createJobScheduler<P0JobRecord>({
  maxConcurrentJobs: schedulerDefaults.maxConcurrentJobs,
  maxConcurrentJobsPerBook: schedulerDefaults.maxConcurrentJobsPerBook,
  async run(job) {
    await runModelBackedJob(requireJob(job.id), job.input.skipAlreadyExtracted ? undefined : { skipAlreadyExtracted: false });
  },
  async onQueued(job, reason) {
    const latestJob = requireJob(job.id);
    await updateJob(latestJob, {
      progressText:
        reason === "book_limit"
          ? schedulerDefaults.queuedByBookLimitText
          : schedulerDefaults.queuedByGlobalLimitText
    });
  }
});
```

如果保留 `activeJobRuns` 用于同 job Promise 去重，只在 `runModelBackedJob` 真正运行时设置；不要用它作为全局并发锁。

- [ ] **Step 5: 替换 start/resume/restart 调用**

新增 helper：

```ts
async function enqueueModelBackedJob(
  job: P0JobRecord,
  runOptions?: RunModelBackedJobOptions
): Promise<JobDto> {
  const scheduledJob = withRunOptions(job, runOptions);
  await jobScheduler.enqueue(scheduledJob);
  return await buildJobDto(requireJob(job.id));
}
```

将 IPC handler 改成：

```ts
"jobs:start": async (input) => {
  const job = requireJob(input.jobId);
  return enqueueModelBackedJob(job);
},
"jobs:resume": async (input) => {
  const job = requireJob(input.jobId);
  return enqueueModelBackedJob(job);
},
"jobs:restart": async (input) => {
  const job = requireJob(input.jobId);
  return enqueueModelBackedJob(job, { skipAlreadyExtracted: false });
},
```

- [ ] **Step 6: 删除排队任务时同步移出队列**

在 `"jobs:delete"` 中，删除持久化记录前调用：

```ts
jobScheduler.remove(input.jobId);
```

若 `jobScheduler.isRunning(input.jobId)` 为 true，抛出明确错误：

```ts
throw new Error("运行中的任务不能删除，请先等待任务完成或暂停。");
```

该错误只影响绕过 UI 直接调用 IPC 的情况；当前 UI 本来不会对 running 暴露删除按钮。

- [ ] **Step 7: 更新旧同步测试的等待方式**

在 `p0Handlers.test.ts` 增加 helper：

```ts
async function waitForJobStatus(
  contract: ReturnType<typeof createIpcContract>,
  handlers: ReturnType<typeof createHandlers>,
  projectId: string,
  jobId: string,
  status: JobDto["status"]
): Promise<JobDto> {
  let matchedJob: JobDto | undefined;
  await vi.waitFor(async () => {
    const runtime = await contract.invoke(handlers, "projectRuntime:get", { projectId });
    matchedJob = runtime.jobs.find((job) => job.id === jobId);
    expect(matchedJob?.status).toBe(status);
  });
  return matchedJob as JobDto;
}
```

把依赖 `const completedJob = await contract.invoke(... "jobs:start" ...)` 的断言改成：

```ts
await contract.invoke(handlers, "jobs:start", { jobId: job.id });
const completedJob = await waitForJobStatus(contract, handlers, "project-a", job.id, "completed");
```

- [ ] **Step 8: 运行桌面端主进程测试**

Run:

```powershell
rtk pnpm --filter @novel-extractor/desktop test -- jobScheduler.test.ts p0Handlers.test.ts
```

Expected: PASS。

---

### Task 4: Renderer 行为回归

**Files:**
- Modify: `apps/desktop/src/renderer/features/extraction/ExtractionPage.test.tsx`
- Modify: `apps/desktop/src/renderer/App.test.tsx`

- [ ] **Step 1: 写任务列表排队展示测试**

在 `ExtractionPage.test.tsx` 增加：

```tsx
it("shows queued progress text in the pending job card", () => {
  render(
    <ExtractionPage
      books={[]}
      jobs={[
        {
          id: "job-queued",
          status: "pending",
          progressText: "等待同书任务完成",
          createdAt: "2026-07-05T00:00:00.000Z"
        }
      ]}
      models={[modelForTest]}
      state="ready"
    />
  );

  expect(screen.getByText("等待同书任务完成")).toBeInTheDocument();
  expect(screen.getByRole("button", { name: "待开始" })).toBeDefined();
});
```

如果当前测试环境里状态不是 button 文本，改用现有 job card 查询方式：

```tsx
expect(screen.getByText("待开始")).toBeInTheDocument();
```

Expected: PASS 或暴露现有选择器差异；不需要新增 UI 组件。

- [ ] **Step 2: 写 App 异步启动回归测试**

在 `App.test.tsx` 增加：`api.startJob` 返回 running job，随后通过 `onJobUpdated` 推送 completed job，页面应更新为完成。

```tsx
it("keeps the optimistic running job and accepts later job update events", async () => {
  const user = userEvent.setup();
  const api = createApiMock();
  const listeners: Array<(job: JobDto) => void> = [];
  api.onJobUpdated.mockImplementation((handler) => {
    listeners.push(handler);
    return () => undefined;
  });
  api.getProjectRuntime.mockResolvedValue({
    books: [],
    jobs: [
      {
        id: "job-async",
        bookId: "book-1",
        status: "created",
        progressText: "进度：0/1",
        allowedActions: ["start", "delete"],
        createdAt: "2026-07-05T00:00:00.000Z",
        updatedAt: "2026-07-05T00:00:00.000Z"
      }
    ]
  });
  api.startJob.mockResolvedValue({
    id: "job-async",
    bookId: "book-1",
    status: "running",
    progressText: "正在准备运行窗口",
    allowedActions: ["pause"],
    createdAt: "2026-07-05T00:00:00.000Z",
    updatedAt: "2026-07-05T00:00:01.000Z"
  });

  render(<App />);
  await user.click(await screen.findByRole("button", { name: "开始" }));
  expect(await screen.findByText("正在准备运行窗口")).toBeInTheDocument();

  listeners[0]?.({
    id: "job-async",
    bookId: "book-1",
    status: "completed",
    progressText: "进度：1/1",
    allowedActions: ["delete"],
    createdAt: "2026-07-05T00:00:00.000Z",
    updatedAt: "2026-07-05T00:02:00.000Z"
  });

  expect(await screen.findByText("已完成")).toBeInTheDocument();
});
```

如果 `App.test.tsx` 已有 `createApiMock` 名称不同，使用该文件现有 mock factory；断言语义保持一致。

- [ ] **Step 3: 运行 renderer 测试**

Run:

```powershell
rtk pnpm --filter @novel-extractor/desktop test -- ExtractionPage.test.tsx App.test.tsx
```

Expected: PASS。

---

### Task 5: 全量验证和边界清理

**Files:**
- Review: `apps/desktop/src/main/p0Handlers.ts`
- Review: `apps/desktop/src/main/jobScheduler.ts`
- Review: `packages/config/src/defaults.ts`
- Review: `packages/config/src/configInvariants.ts`

- [ ] **Step 1: 静态检查硬编码**

检查生产代码中不能散落这些硬编码判断：

```powershell
rtk rg -n -S '等待可用运行槽|等待同书任务完成|maxConcurrentJobs: 2|maxAllowedConcurrentJobs: 3|maxConcurrentJobsPerBook: 1' apps packages
```

Expected:

- 文案和数字只允许出现在 `packages/config/src/defaults.ts`、配置测试、计划文档和调度器单元测试夹具中。
- `p0Handlers.ts` 必须通过 `getDefaultConfig().jobSchedulerDefaults` 或 `options.jobSchedulerDefaults` 读取策略。

- [ ] **Step 2: 检查同书锁不会进入窗口内部并行**

运行：

```powershell
rtk rg -n -S 'Promise\\.all\\(|for \\(const \\[.*windowIndex|for \\(const manifestWindow' apps/desktop/src/main/windowRunService.ts packages/jobs/src/jobRuntime.ts
```

Expected:

- `windowRunService.ts` 的窗口循环仍是串行 `for`。
- 本次不新增窗口级 `Promise.all`。

- [ ] **Step 3: 运行目标测试**

Run:

```powershell
rtk pnpm --filter @novel-extractor/config test
rtk pnpm --filter @novel-extractor/desktop test -- jobScheduler.test.ts p0Handlers.test.ts ExtractionPage.test.tsx App.test.tsx
```

Expected: PASS。

- [ ] **Step 4: 运行类型检查**

Run:

```powershell
rtk pnpm -r typecheck
```

Expected: PASS。

- [ ] **Step 5: 检查工作树差异**

Run:

```powershell
rtk git diff -- packages/config/src/schema.ts packages/config/src/defaults.ts packages/config/src/configInvariants.ts apps/desktop/src/main/jobScheduler.ts apps/desktop/src/main/p0Handlers.ts
rtk git status --short
```

Expected:

- diff 只包含调度配置、调度器、p0Handlers 接入和对应测试。
- 不包含无关重构、格式化整文件或 release 产物。

## 自审记录

- Spec coverage: 已覆盖默认并行 2、配置上限 3、同书互斥、跨书并行、自动排队、单任务内部串行和启动 IPC 异步返回。
- Placeholder scan: 本计划没有占位标记、未定义步骤或空泛“补充测试”。
- Type consistency: 计划中的 `JobSchedulerDefaults`、`ScheduledJob`、`QueueBlockReason`、`createJobScheduler` 名称在任务中定义后再使用。
- 配置化检查: 并行数、同书上限和排队文案均从 `packages/config` 进入生产逻辑。
- 简化检查: 第一版不引入供应商限速、窗口并行、队列 UI 新状态或设置页，避免扩大改动面。
